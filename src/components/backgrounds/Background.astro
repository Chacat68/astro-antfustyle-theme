---
/**
 * 全站背景调度：统一渲染 Glitch。
 * `type` 仍接受旧枚举（dot/plum/…）以兼容 frontmatter，实际均映射为 glitch。
 */
import Glitch from '~/components/backgrounds/Glitch.astro'

import type { BgType } from '~/types'

interface Props {
  type: BgType
}

const { type: _type } = Astro.props
---

<Glitch />
